# NOTE: inferred from src/ file names — please verify against what's
# actually installed in your venv with: pip freeze > requirements.txt
pypdf>=3.0.0
spacy==3.7.0
langchain-google-genai
langchain-core
pydantic==2.0.0
openai==1.0.0
groq>=0.5.0
streamlit==1.28.0
scikit-learn==1.3.0
pandas==2.0.0
pytest==7.4.0

# For api.py (FastAPI backend) — confirmed required by actually running it:
fastapi
uvicorn
python-multipart
